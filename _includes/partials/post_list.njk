        <section id="blog" class="py-5 bg-light">
            <div class="container">
                <div class="text-center p-3">
                <h3 class="custom-heading"><strong><a class="none" href="{{page.url}}">{{widget.blog.title}}</a></strong></h3>
            <p>{{widget.blog.description}}</p>    
            </div>
                <div class="row">
                    {% for post in collections.posts | limit(3) %}<div class="col-md-4 mb-4">
                        <div class="card h-100 custom-card">
                            <img src="{{post.data.image or metadata.image}}" class="card-img-top custom-blog-img" alt="{{post.title}}" 
                            loading="lazy" width="100%" height="100%">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title fw-bold">{{post.data.title}}</h5>
                                <p class="card-text custom-text-sm flex-grow-1">
                                    {{post.data.description}}</p>
                                <a href="{{post.url}}" 
                                class="mt-auto stretched-link text-decoration-none fw-bold">
                                {{post.data.title}} →</a>
                            </div>
                        </div>
                    </div>{% endfor %}
                    <p class="col-md-12 text-center p-3 mt-3 mb-3">
                        <a href="{{widget.blog.button.url}}" 
                        class="btn btn-dark btn-lg custom-btn col-12 col-md-4">{{widget.blog.button.text}}</a>
                    </p>
                </div>
            </div>
        </section>